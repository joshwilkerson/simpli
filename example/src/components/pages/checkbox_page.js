import React from "react"
import Layout from "../layout"
import { Button } from "simpli"

const CheckboxPage = () => {
	return (
		<Layout>
			<Button onClick={() => console.log("not a button")} />
		</Layout>
	)
}

export default CheckboxPage
